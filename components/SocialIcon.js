import React from "react";

const SocialIcon = ({ icon }) => {
  // eslint-disable-next-line import/no-dynamic-require
  // const Icon = require(`icons/${icon}.svg?sprite`);
  // return <Icon style={{ width: 16, height: 16 }} />;
};

export default SocialIcon;
